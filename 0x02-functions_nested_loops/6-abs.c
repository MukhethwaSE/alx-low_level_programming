#include "main.h"
#include <stdlib.h>

/**
 * _abs - The program computes the absolute value of an integer
 * @n: The integer number to compute
 * Return: The absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}


