#include <stdio.h>

/**
 * main - The program prints all single digit numbers of base 10 starting from 0.
 *
 * Return: 0
 */
int main(void)
{
	for (int i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
